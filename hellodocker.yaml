apiVersion: v1
kind: Pod
metadata:
  labels:
    run: hello-docker
  name: hello-docker
  namespace: foo
spec:
  containers:
  - image: qualitto007/service-registry:latest
    name: hello-docker
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Never
status: {}
